version: "2"
sql:
  - schema: "db/schema"
    queries: "db/query"
    engine: "sqlite"
    gen:
      go:
        package: "model"
        out: "model"
        sql_package: "database/sql"
        emit_json_tags: true
        emit_interface: true
        overrides:
          - db_type: "DATE"
            go_type: "string"
